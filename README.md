# CompilerBackend
to compile:
```bash
git clone https://github.com/radinParsaei/CompilerBackend --recursive
cd CompilerBackend
cd VM
make
cd ..
make
```

run:
```bash
java -jar output.jar
VM/assembler a
VM/VM out.bin
```
